-- ============================================================================
-- 1035 のロールバック: 組織管理者向け SELECT ポリシーを削除
-- ============================================================================
-- 戻すと、契約者以外の管理者はプラン変更・解約ができなくなり、
-- upgrade ページの重複契約ガードも素通りする状態に戻る（監査 P0-A）。
-- API 側（/api/stripe/upgrade-organization）のガードは service role で判定するため
-- ロールバック後も有効。
-- ============================================================================

drop policy if exists "authed_subscriptions_select_by_org_admin" on public.subscriptions;
