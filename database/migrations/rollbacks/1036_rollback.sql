-- ============================================================================
-- 1036 のロールバック: results.created_at のデフォルトを外す
-- ============================================================================
-- 戻すと、以後の採点日時が再び記録されなくなる（エクスポートの日時列が壊れる）。
-- 既に記録された行の値は消さない。
-- ============================================================================

alter table public.results
  alter column created_at drop default;
